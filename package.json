{
  "name": "geest",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"yarn dev:web\" \"yarn dev:api\"",
    "dev:web": "yarn --cwd web dev",
    "dev:api": "yarn --cwd api start:dev",
    "build": "yarn build:web && yarn build:api",
    "build:web": "yarn --cwd web build",
    "build:api": "yarn --cwd api build",
    "preview": "yarn --cwd web preview",
    "lint": "yarn --cwd web lint && yarn --cwd api lint",
    "lint:fix": "yarn --cwd web lint:fix && yarn --cwd api lint:fix",
    "format": "yarn --cwd web format && yarn --cwd api format",
    "format:check": "yarn --cwd web format:check && yarn --cwd api format:check",
    "setup": "yarn --cwd web install && yarn --cwd api install",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "db:logs": "docker compose logs -f mongo",
    "db:seed": "yarn --cwd api seed"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
